# 変更履歴

[English](CHANGELOG.md) | **日本語**

このプロジェクトの注目すべき変更はすべてこのファイルに記録します。

## [Unreleased]

## [2.5.0] - 2026-08-28

### 追加

- タイトル・章扉・大メッセージのスライドで、文字サイズをコンテンツ量に応じて自動的に縮小するように追加
- AI生成結果が自動修正ループ後も検証エラーを含む場合、そのまま適用せず警告と「再生成する」ボタンを表示するように追加

### 修正

- 「見た目をチェックして修正」でAIが指摘対象外のフィールド（speakerNotes等）を書き換えてしまう不具合を修正
- AI生成でLLM呼び出し自体が一時的に失敗（モデルからの空応答等）した際、既存の自動修正ループで再試行されず即座に失敗していた問題を修正
- 拡張思考がトークン予算を使い切ってしまい、AIスライド生成が空応答で失敗することがある不具合を修正

## [2.4.0] - 2026-08-27

### 追加

- `meta.logo` と対称なデッキ全体設定 `meta.confidential`（Confidential等の透かし文言）を追加
- `slides[].meta` 経由で `meta.logo`/`meta.confidential` をスライド個別に上書き（非表示も含む）できるように追加
- AI生成パネルの「Generate」の隣に「見た目をチェックして修正」ボタンを追加。CIと同じ視覚チェックを全スライドに実行し、問題があれば既存の自動修正ループでAIに修正を依頼するように

### 変更

- `meta.confidential` の既定位置を `bottom-left` から `top-right` に変更し、`anchor`/`offset` を指定せずにロゴと透かしを両方使っても既定で重ならないように（`meta.logo` の既定位置は `bottom-left` のまま）
- 見た目チェックに、内側コンテナ自身のoverflowによる見切れの検知を追加し、既存のはみ出し・セーフエリア侵入・装飾との重なり・高さ0の埋める要素の検知を強化

### 修正

- 自動更新チェックがGitHub APIのレート制限で無言で失敗する問題を修正
- 配布サンプルの一部ガイドスライドの見切れを修正

## [2.3.1] - 2026-08-26

### 追加

- スライドタイトルで `\n` による改行位置を明示的に指定できるように追加し、長いタイトルの折り返し位置を著者が制御できるように

### 修正

- 表を含むスライドでPDF書き出しが失敗する不具合を修正

## [2.3.0] - 2026-08-24

### 追加

- プレゼンテーション録画機能を追加: ツールバーのボタンから画面/ウィンドウ共有とスピーカーノート音声（voice再生音）をまとめて録画し、動画ファイルとして保存できるように

## [2.2.0] - 2026-08-19

### 追加

- 配布用テーマファイル（PowerPoint / Google スライド由来）を取り込み、マスターの配色・フォント・ロゴ・ページ番号・固定テキストを決定的に抽出し、取り込み前に並置比較・確認できる「ブランドテーマ取り込み」機能を追加
  - 複数のマスター/レイアウトへの対応、ライト/ダークの明示指定、埋め込みフォントの同梱、マスター内の小図形からのブランドマーク候補抽出、レイアウト割り当ての初期値提示に対応
- ブランドテーマだけを単体で書き出し・配布できる形式を追加
- マスター背景の装飾語彙を拡張（背景パターン・透かし・不透明度・画像背景・固定テキストとページ番号・章フッター）
- 章（section）の概念を追加し、章番号・章単位のアクセント色切り替えに対応
- 新しいスライド種別を多数追加: 表、チャート（棒・折れ線・円・横棒・大数値＋推移・KPI・KPI行）、図解プリミティブ（矢印・コネクタ・カード・バッジ）、比較・横フロー、目次、構成図（階層・サーバ構成・組織図・UMLクラス図）、プロセス図（フローチャート・スイムレーン・日付タイムライン・ガント）、分析図（2×2マトリクス・ファネル・SWOT・ヒートマップ）、画像（自動フィット・キャプション・枠・グループ化・カテゴリ見出し）、プロフィール、UMLシーケンス図、SVGインライン取り込み・テキスト図法（Mermaid）、引用・大メッセージ・締め、チェックリスト・番号付き多列リスト
- 日付タイムラインに列数指定（dateTimelineColumns）を追加し、8件を超える項目でも1枚に折り返して収められるように
- テーマの意匠トークンを拡充: カード/罫線の角丸・線幅・アクセント・ゼブラ、見出し下線の線幅、装飾的な太線（見出し左バー・上端帯・区切り線・ノードリング）、系列色の明示的な濃淡ランプ、円・正方形の装飾、キャンバスサイズとセーフエリアのテーマ化、テーマ色12キー全反映（系列色・状態色・リンク色を含む）
- 見た目の自動検証（はみ出し・セーフエリア侵入・マスター装飾との重なり・コントラスト不足）をアプリ内で検出し、編集中に警告表示するように
- 不正な指定（KPI/チャートの色トークン名、図解のrow/col/startColの範囲外・非整数値）を警告として通知するように
- スライド1枚の描画エラーがデッキ全体をクラッシュさせないよう、エラーバウンダリを追加
- スライド画像を遅延読み込みにし、表示負荷を軽減

### 変更

- AI生成結果をテーマ単位・スライド単位で選んで部分適用できるように変更
- AI生成の指示が新規作成か既存内容の変更かを明示するモードを追加するように変更
- AI生成がトークン上限で途中切断された場合を検知し、自動修正ループへ伝えるように変更
- AI生成にテーマ由来の意匠制約（配色・フォント等）を反映するように変更
- マスター背景の生成・優先順位・既定色の与え方を統一し、これまでの不整合な挙動を解消
- ブランドテーマ取り込みボタンを「テーマ」見出しの右隣に配置するよう変更

### 修正

- 自動アップデートの確認先URLがdraft状態のuntagged URLを指し、更新を検出できない不具合を修正
- 配布サンプル（テンプレートガイド）の見た目破綻5件、アプリの言語設定を無視する不具合、セーフエリア侵入を修正
- PDF書き出しでentrance animationの最終状態がhtml2canvasのクローン内で確定せず、表示が崩れる不具合を修正
- スライドタイトルの下マージンがMUIの動的CSSに上書きされる不具合を修正
- 図解スライドでrow/col/startColに範囲外・非整数を指定すると例外で描画が落ちる不具合を修正
- ブランドテーマ取り込みでライト/ダークを明示指定できない不具合を修正
- 書体のCSS変数が二重管理され、意図しないフォントが適用される不具合を修正
- ロゴの表示をマスター装飾語彙（LogoMasterDecoration）に統合し、meta.logoとマスター側装飾の不整合を解消

## [2.1.1] - 2026-08-02

### 追加

- 外部生成（Claude Code CLI）方式でClaude CLI環境変数（`CLAUDE_CONFIG_DIR` 等）を設定できるように

### 変更

- AI生成結果の差分表示を、2カラムの全文比較からgit diff風の行単位表示（追加=緑／削除=赤）に変更

### 修正

- 差分ダイアログで長い行によりCSS Gridの変更後カラムが見えなくなる不具合を修正
- 外部生成（Claude Code CLI）方式の失敗時に、具体的な失敗理由を表示するように修正
- 発表者ビューを本編再生中に開くと、常に先頭スライドから表示される不具合を修正
- 外部生成CLIのツール呼び出しによる失敗と、グローバルCLAUDE.mdの意図しない混入を修正

## [2.1.0] - 2026-07-30

### 追加

- 自動アップデート（GitHub API 経由の起動時チェック）を追加
- 音声読み上げの一時停止・再開機能を追加
- スライドを PDF として書き出せるツールバーボタンを追加
- 編集画面から設定ダイアログを開けるように
- 初回起動時にホーム画面から設定を開いて UI 言語を変更できるように

### 変更

- キーボードショートカット一覧をアプリ内ダイアログに一本化し、Reveal.js 組み込みヘルプとの競合を解消
- UI テーマカラーをアプリロゴの配色（オレンジ→ミントグリーン/スレート）に更新

### 修正

- macOS で PDF 書き出しボタンが無反応になる不具合を修正
- 音声の一時停止・再開時にシークバーが 0% からリスタートする不具合を修正
- 編集画面のスライドプレビューが左寄せになる不具合を修正
- スライドパッケージ名の検証でアンダースコアが使えない不具合を修正

## [2.0.0] - 2026-07-28

Slide Presentation App はデスクトップアプリになりました。閲覧するだけでなく、アプリ内でスライドの作成・生成・パッケージ化
ができます。

### 破壊的変更

- **デスクトップアプリへ移行** — ブラウザで動く閲覧ツールから、macOS・Windows・Linux 向けの Tauri 2（Rust）製デスクトップ
  アプリになりました。配布は GitHub Releases のインストーラ（`.dmg`、`.msi` / `-setup.exe`、`.deb` / `.rpm` /
  `.AppImage`）です
    - 起動すると同梱デッキをそのまま描画するのではなく、何を表示するかを選ぶ **ホーム画面** から始まります
    - 発表者ビューはブラウザのポップアップと `BroadcastChannel` ではなく、Tauri イベントで同期するネイティブウィンドウに
      なりました
    - ビルド時同梱（`public/slides.json`・`VITE_SLIDE_PACKAGE`）は従来どおり使えます
- **スライドパッケージの拡張子** — 書き出すスライドパッケージの拡張子を `.tgz` から独自拡張子 `.spkg` に変更（内部形式は
  同じ tar+gzip）。パッケージを開く側は後方互換として旧 `.tgz` ファイルも引き続き開けます。CLI での書き出し
  （`npm run export:slides`）は `npm pack` の出力をリネームして対応しますが、その結果、書き出し直後のパッケージを
  ローカル npm tarball として `npm install ./dist-slides/xxx.tgz` する使い方は非対応になりました。代わりに
  `VITE_SLIDE_PACKAGE` のローカルパス指定を使ってください（[スライドパッケージ](README.ja.md#スライドパッケージ) 参照）

### 追加

- **ホーム画面** — 起動時に何を表示するかを選べます。ツールバーの **ホーム** ボタンでいつでも戻れます
    - **AIで新規作成**（ゼロから生成）・**ファイルを開く**（`slides.json` / `.spkg`）・**URL から開く**（HTTPS で `.spkg`
      を取得し、ローカルにキャッシュ）・**サンプルを開く**・**最近開いたスライド**（起動をまたいで永続化し、項目ごとに
      削除できます）
    - 最後に開いたファイルを記憶し、次回起動時に自動で読み込みます
- **`.spkg` のファイル関連付け** — OS のファイルマネージャからスライドパッケージを直接開ける（ダブルクリック、または
  右クリック→このアプリで開く）。macOS・Windows・Linux に対応
    - アプリが起動していない場合はそのまま起動してホーム画面を経由せずプレゼンテーションを表示。すでに起動している
      場合は既存のウィンドウで開き直す（ウィンドウは増えない）
    - 編集モードで未保存の変更がある場合は先に確認ダイアログを表示し、内容が黙って破棄されないようにする
    - キャッシュ展開・相対アセット解決（`image/`・`voice/`・`theme/`・`font/`）・同梱アドオンの信頼確認・最後に開いた
      ファイルの自動再読込は、ホーム画面から開いた場合とまったく同じように動作する
    - OS へ登録するのは `.spkg` のみ。旧 `.tgz` パッケージはホーム画面から引き続き開ける
- **URL から開く** — HTTPS で `.spkg` を取得する既存のホーム画面アクションをドキュメント化
- **編集モード** — アプリ内でスライドの編集とパッケージ化ができる（ツールバーの **編集** ボタンで切替）
    - メタ情報フォーム＋全幅の `slides.json` エディタが単一の真実源を共有し、本番と同一レンダラのライブプレビューを表示
      （テーマ編集は即時反映）
    - JSON エディタは行番号・シンタックスハイライト・検索 / 置換（`Ctrl`/`Cmd` + `F`）に対応
    - 編集した `slides.json` のローカル保存と、`.spkg` スライドパッケージの書き出し（名前 / バージョン、参照アセットを同梱）
    - バリデーション連動: JSON に構文・スキーマエラーがある間は保存 / 書き出しを無効化しプレビューを非表示
    - 未保存の変更があるまま編集モードを終了しようとすると確認ダイアログを挟む
    - ファイル書き込みは編集モード中のみ、Rust 境界で実行（最小権限）
- **AI スライド生成** — 編集モードの **AI 生成** パネル（またはホーム画面の **AIで新規作成**）からプロンプトで `slides.json` を生成し、差分確認ダイアログで承認して器へ流し込む
    - 内蔵（Vertex AI・GCP ADC 認証／project・region・model を設定）／外部（ローカル `claude` CLI）を同一契約で切り替え
    - 認証は GCP ADC（`gcloud auth application-default login`）。アクセストークンは Rust 境界で取得（約 55 分キャッシュ）し Web 層へ非開放（最小権限）
    - 取り込み検証＋自動修正ループ（上限 3 回・上限時は最良候補を退避）、進捗表示・中断・失敗時の手動編集への安全退避
    - 適用前に差分確認ダイアログで構造変更（追加/変更/削除スライドとメタ変更）をプレビューし **適用する** / **キャンセル** を選択（キャンセルは器に触れない）。適用時は JSON を 2 スペースに整形
    - 生成・GCP 設定/ログインは編集モード かつ 生成有効時のみ有効化（事前ゲート: Vertex 設定未完了／CLI 未検出で無効化）
- **アドオンの付け外し（3 層）** — 同梱される実行アドオンの制御
    - パッケージ単位の実行時信頼（開いた時の確認ダイアログ＋設定、既定は拒否／グローバル無効化が優先）
    - 書き出し同梱の選択（アプリ内 **同梱アドオン** チェックボックスと `npm run export:slides --addons a,b`）。候補はパッケージのアドオンと dev の組み込みアドオンの和集合で、候補が無くても一覧は消えず空状態を明示
    - 開発ビルド限定の組み込み `addons/src/<名前>/entry.ts` の増減（削除は確認ダイアログを挟みます。ソースを完全削除し git 管理外のため取り消せません）。加えてアプリ内 **ビルド** ボタン（`npm run build:addons` を実行）で、追加/削除した組み込みアドオンをターミナルなしで同梱候補へ反映
- **キーボードショートカットとツールバーの表示切替** — `?`（または **設定 → キーボードショートカット**）でビューア / 編集
  モードのショートカット一覧を表示。ツールバーはボタンまたは `T` キーで完全に隠せます
- **進捗と失敗のフィードバック** — ファイル / パッケージを開く間のローディング表示と、これまで黙って失敗していた処理
  （テーマ適用・発表者ビューの起動・サンプル取得）のトースト通知

### 変更

- **サンプルスライドの外部配布** — テンプレートガイドのサンプルをアプリに同梱せず、GitHub Releases の `.spkg`
  パッケージとして配布し、**サンプルを開く** で取得する（[配布サンプル](README.ja.md#配布サンプル) 参照）
    - 取得は 3 段: ビルド時同梱の `slides.json` → バージョン固定のリリースアセット → `latest` のリリースアセット。
      どこからも取得できない場合はネットワーク接続を促す 1 枚を表示（アプリの起動自体には影響しません）
    - バージョン固定アセットは内容が不変なため展開結果をキャッシュし、2 回目以降はオフラインでも開けます
    - サンプルの音声がアプリのバンドルから外れ、サンプルの更新をアプリのリリースから切り離せるようになりました
    - フランス語のサンプルを追加（UI は fr-FR 対応済みでしたが、サンプルは英語版が表示されていました）
    - `VITE_SAMPLE_PACKAGE_URL` / `VITE_SAMPLE_SOURCE=remote` で取得元を差し替えられます
    - `npm run export:samples` で全ロケール分を書き出せます。`npm run export:slides` に `--source`（入力元ディレクトリ）と
      `--strict`（参照アセット欠損で失敗）を追加
- **設定画面と編集画面の chrome** — 設定ウィンドウを本物のモーダルダイアログにしました（`Esc` で閉じる・フォーカス管理・
  `role="dialog"`）。設定画面と編集画面の chrome は固定パレットを使い、開いているスライドのテーマ色を継承しません
- **アクセシビリティ** — アイコンボタン・トグル・チェックボックスのラベルと押下状態の公開を統一

### 修正

- **配布サンプルの見切れ** — 情報を詰め込みすぎていた 2 枚を分割し、1280x720 での見切れを解消
    - 「発表者ビュー」を概要とパネル一覧の 2 枚に分割（右カラムが縦に 52px あふれ、見出しが切れページ番号と重なっていた）
    - 「ロゴ・フォント設定」を「ロゴ設定」「フォント設定」の 2 枚に分割（縦に 237px あふれ、コードブロックの末尾が
      途切れていた）
- **フランス語サンプルの見切れ** — 「テーマ設定の詳細」でフランス語のみ折り返していた 2 行を短縮し、1280x720 で右カラムが
  縦に 43px あふれていたのを解消（`slides.fr.json` のみ。ja / en は元から収まっていた）
- **タイムラインがスライドからはみ出す** — `steps` のタイムラインが親の左右へ 100px ずつ食い込む一方、`.content-area` の
  左右 padding は 40px しかなく、1280x720 のスライドから左右 60px はみ出して両端のステップのテキストが欠けていた。食い込み量を
  padding と同じ `--content-area-padding-x` から導出し、タイムラインがスライド幅ぴったりに収まるようにした
- **サンプルの壊れた参照** — 実体のない音声ファイル（英語）とログファイル（`/demo-log.txt`）への参照を除去
- **発表者ビューの信頼性** — ウィンドウのクローズを `onCloseRequested` で検知し、ウィンドウの起動失敗は黙って失敗する
  代わりにトーストで通知するようにしました。ローカルで開いたパッケージのテーマも発表者ビューへ伝搬します
- **AI の自動修正が上限に達したときに検証エラーが消えない** — 上限到達時、残っている検証エラーを差分確認ダイアログに
  表示します
- **組み込みアドオンが release ビルドに混入しない** — 開発ビルド限定の組み込みアドオンを dev ビルドにゲートし、
  リリースへ焼き込まれないようにしました
- **書き出したパッケージが入力した名前のままになる** — `.spkg` のファイル名に `slides-` プレフィックスが付いていました
- **プレゼンテーションが 1 枚目から始まる** — スライドを開く前に URL ハッシュをリセットし、前に見ていたデッキの位置へ
  ジャンプしないようにしました
- **テーマ・ダイアログ・一覧の修正** — `theme-colors.json` が無い / パースできない場合をエラーではなく「上書きなし」として
  扱い、最近開いたスライドの一覧はパスが読める形でスクロールし、パッケージ単位のアドオン信頼一覧は実際に信頼判断がある
  パッケージのみに限定（長い場合はスクロール）

## [1.0.0] - 2026-02-02

初回リリース。スライドを JSON データで定義してブラウザに表示するプレゼンテーションツールです。

### 追加

- **5 種類のレイアウト** — `layout` フィールドで `center` / `content` / `two-column` / `bleed` / `custom` を切り替え
- **テーマのカスタマイズ** — slides.json の `theme` フィールド（色・フォント・カスタム CSS）でカスタマイズ、または
  `theme-colors.json` で色を上書き
    - カスタムフォントの読み込み（ローカルファイル / Google Fonts）
    - `baseFontSize` によるフォントサイズの一括スケール
    - すべての色を CSS 変数として適用（`rgba()` 対応の `-rgb` バリアント付き）
- **音声ガイド & 自動送り** — スライドごとに音声ファイルを割り当て
    - 手動再生（スピーカーアイコンで再生 / 停止）
    - 自動再生（スライド遷移時に音声を自動再生）
    - 自動スライドショー（音声終了時に次のスライドへ自動送り）
- **発表者ビュー** — 専用の発表者ウィンドウを開く
    - スピーカーノート、要点サマリー、次 / 前スライドのプレビュー
    - BroadcastChannel によるメインウィンドウとの双方向リアルタイム同期
    - キーボードナビゲーション（矢印キー / スペース）
    - 発表者ビューからの音声コントロール
- **国際化 (i18n)** — 日本語（`ja-JP`）/ 英語（`en-US`）/ フランス語（`fr-FR`）
    - ブラウザの言語設定からの自動検出
    - 設定ウィンドウからの手動切り替え（`localStorage` に保存）
- **アドオンシステム** — カスタムコンポーネントをアドオンとして追加・登録
- **スライドパッケージ** — スライド内容を npm パッケージとしてエクスポート・インポート・配布
    - `npm run export:slides` で `.tgz` パッケージを生成（参照アセットを自動検出して同梱）
    - `VITE_SLIDE_PACKAGE` 環境変数でインポート（ローカルパス / npm パッケージ）
- **ロゴ設定** — `meta.logo` フィールドでプレゼンテーションのロゴをカスタマイズ
- **スライドメタ** — トランジション、背景色、背景画像を制御

### 技術スタック

| カテゴリ            | 技術                       |
|---------------------|----------------------------|
| フレームワーク      | React 19                   |
| プレゼンエンジン    | Reveal.js 5                |
| UI コンポーネント   | MUI (Material UI) 7        |
| ビルドツール        | Vite 7                     |
| 言語                | TypeScript 5（strict モード）|
| テスト              | Vitest 4                   |
| コードフォーマッタ  | Prettier                   |
